%h1= glyph_and_text HarbourCrane::Application::Glyph::CONTAINER, "Containers listing"
%p List of containers running in your server
%br
%br
= button_link 'Refresh', state: :success, glyph: 'refresh', url: containers_path
%br
%br
%table.table
  %tr
    %th Container ID
    %th Image
    %th Command
    %th Created at
    %th Status
    %th.container_ports Ports
    %th Names
    %th Actions
  - @containers.each do |container|
    %tr
      %td= container.display_id
      %td= container.image_name
      %td= container.command
      %td= container.created_at
      %td= container.status
      %td= container.ports
      %td= container.names
      %td
        - unless container.image.proxy? || container.image.administration?
          = link_to glyph('eye'), container_path(container.id)
          = link_to glyph('square'), stop_container_path(container.id)
